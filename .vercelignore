# Development files
*.log
*.md
.git
.gitignore

# Admin files (not needed for deployment)
admin/

# Server source files (not needed, only compiled dist is needed)
server/src/
server/node_modules/
server/scripts/
server/data/
server/nodemon.json
server/package-lock.json

# Keep server/dist and server/wasm-equity for API functions
# Keep server/package.json for dependencies

# Development configs
nodemon.json

# Docker files
docker-compose.yml
clickhouse-init/
grafana/
prometheus.yml
otel-collector-config.yaml
otel-collector-logs/

# Environment files (will be set in Vercel dashboard)
.env
.env.*
!.env.example

# Node modules (will be installed during build)
node_modules/

# Development tools (not needed for build)
web/plopfile.js
web/plop/

# IMPORTANT: Source files and configs are NEEDED for build
# We explicitly include them by NOT excluding them
# - web/src/ (needed for webpack)
# - web/public/ (needed for webpack to copy index.html)
# - common/src/ (needed for TypeScript compilation)
# - tsconfig.json files (needed for TypeScript)
# - webpack.config.cjs (needed for webpack)
# - package.json files (needed for npm install)
