{
  "name": "ship-inspector",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd common && npm install && npm run build && cd ../web && npm install && npm run build",
    "build:web": "cd web && npm install && npm run build",
    "build:admin": "cd admin && npm install && npm run build",
    "build:common": "cd common && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

