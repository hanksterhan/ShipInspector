{
  "name": "ship-inspector",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/prepare-vercel-config.js && npm run build:common && npm run build:lib && npm run build:api && npm run build:server && npm run build:web",
    "build:api": "cd api && npm install && npm run build",
    "build:web": "node scripts/prepare-vercel-config.js && cd web && npm install && npm run build",
    "build:common": "cd common && npm install && npm run build",
    "build:lib": "cd lib && npm install && npm run build",
    "build:server": "cd server && npm install && npm run build",
    "build:wasm": "cd lib/wasm-equity && bash build.sh",
    "prebuild:wasm": "bash scripts/prebuild-wasm.sh",
    "prepare:vercel": "node scripts/prepare-vercel-config.js",
    "dev:api": "node scripts/dev-api.cjs",
    "mcp": "node mcp/src/index.mjs",
    "test:multiplayer": "cd api && npm test -- --runInBand poker-table",
    "test:mcp": "cd mcp && npm test"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
